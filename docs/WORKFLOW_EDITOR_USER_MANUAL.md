워크플로우 편집기 사용자 메뉴얼

작성일: 2025-01-28  
버전: 1.0  
대상: 모든 사용자 (analyst, ops, admin)

---

📋 목차

1. [워크플로우 편집기 개요](#워크플로우-편집기-개요)
2. [시작하기](#시작하기)
3. [워크플로우 기본 작업](#워크플로우-기본-작업)
4. [노드 타입별 사용법](#노드-타입별-사용법)
5. [노드 관리 기능](#노드-관리-기능)
6. [데이터 조회 기능](#데이터-조회-기능)
7. [워크플로우 구성 요소 관리](#워크플로우-구성-요소-관리)
8. [워크플로우 실행 및 테스트](#워크플로우-실행-및-테스트)
9. [고급 기능](#고급-기능)
10. [문제 해결](#문제-해결)

---

워크플로우 편집기 개요

워크플로우란?

워크플로우는 여러 작업을 순서대로 또는 병렬로 실행하여 복잡한 데이터 처리 작업을 자동화하는 시스템입니다. 각 작업은 노드(Node)로 표현되며, 노드 간의 연결을 통해 데이터가 전달됩니다.

워크플로우 편집기의 주요 기능

- ✅ 시각적 편집: 드래그 앤 드롭으로 노드를 추가하고 연결
- ✅ 다양한 노드 타입: 프롬프트, API, SQL 쿼리, Python 스크립트 등
- ✅ 실시간 데이터 조회: 각 노드의 중간 결과를 실시간으로 확인
- ✅ 자동 의존성 처리: 이전 노드가 자동으로 실행되어 데이터 준비
- ✅ 워크플로우 관리: 폴더로 정리하고 재사용 가능한 템플릿 제공

---

시작하기

1. 워크플로우 편집기 접근

1. 왼쪽 사이드바에서 "워크플로우 관리" 카테고리를 클릭합니다.
2. "워크플로우 편집기" 메뉴를 클릭합니다.
3. 워크플로우 편집기 페이지가 열립니다.

2. 화면 구성 이해하기

워크플로우 편집기는 다음과 같이 구성되어 있습니다:

```
┌─────────────────────────────────────────────────────────┐
│  상단 바: 워크플로우 선택, 저장, 실행 버튼              │
├──────────┬──────────────────────────────┬───────────────┤
│          │                              │               │
│  노드    │    워크플로우 캔버스         │  속성 패널    │
│  팔레트  │    (노드를 배치하고 연결)     │  (노드 설정)  │
│          │                              │               │
│  - 프롬프트                                 - 노드 정보  │
│  - API                                      - 입력 매핑  │
│  - SQL 쿼리                                 - 출력 설정  │
│  - Python                                   - 데이터 조회│
│  - RAG                                      - 실행 결과  │
│  - 기타...                                  - 저장 버튼  │
│          │                              │               │
└──────────┴──────────────────────────────┴───────────────┘
```

3. 첫 워크플로우 만들기

3.1 새 워크플로우 생성

1. 상단의 "워크플로우" 드롭다운에서 "새 워크플로우"를 선택합니다.
2. 워크플로우 이름과 설명을 입력합니다 (선택사항).
3. "저장" 버튼을 클릭합니다.

3.2 첫 번째 노드 추가하기

1. 왼쪽 노드 팔레트에서 원하는 노드 타입을 찾습니다.
2. 노드를 더블클릭하거나 드래그 앤 드롭으로 캔버스에 추가합니다.
3. 노드가 캔버스에 추가되면 자동으로 속성 패널이 열립니다.

3.3 노드 설정하기

1. 속성 패널에서 노드의 이름, 설명 등을 입력합니다.
2. 노드 타입에 따라 필요한 설정을 입력합니다.
3. "적용" 버튼을 클릭하여 설정을 저장합니다.

3.4 노드 연결하기

1. 노드의 출력 핸들(오른쪽)에서 입력 핸들(왼쪽)로 드래그합니다.
2. 연결선이 생성되면 데이터가 자동으로 전달됩니다.

---

워크플로우 기본 작업

워크플로우 선택 및 관리

워크플로우 선택

1. 상단의 "워크플로우" 드롭다운을 클릭합니다.
2. 목록에서 원하는 워크플로우를 선택합니다.
3. 선택한 워크플로우가 캔버스에 로드됩니다.

워크플로우 폴더 관리

1. 워크플로우를 선택한 후, "폴더" 드롭다운을 클릭합니다.
2. 원하는 폴더를 선택하거나 "폴더 없음"을 선택합니다.
3. 워크플로우가 선택한 폴더로 이동합니다.

워크플로우 정보 편집

1. 상단의 "편집" 버튼(연필 아이콘)을 클릭합니다.
2. 워크플로우 이름과 설명을 수정합니다.
3. "저장" 버튼을 클릭합니다.

워크플로우 저장

워크플로우는 자동 저장되지만, 수동으로 저장할 수도 있습니다:

1. 상단의 "저장" 버튼을 클릭합니다.
2. 저장 완료 메시지가 표시됩니다.

💡 팁: 변경사항이 있을 때는 상단에 "변경됨" 배지가 표시됩니다.

워크플로우 삭제

1. 상단의 "삭제" 버튼(휴지통 아이콘)을 클릭합니다.
2. 확인 대화상자에서 "삭제"를 클릭합니다.

---

노드 타입별 사용법

1. 시작 노드 (Start Node)

용도: 워크플로우의 시작점을 나타냅니다.

설정:
- 이름: 워크플로우 시작점
- 설명: (선택사항)

사용 예시:
- 워크플로우의 첫 번째 노드로 사용
- 초기 데이터를 설정할 때 사용

---

2. 프롬프트 노드 (Prompt Node)

용도: AI 프롬프트를 실행하여 텍스트를 생성합니다.

2.1 프롬프트 노드 생성

1. 노드 팔레트에서 "프롬프트" 카테고리를 찾습니다.
2. 등록된 프롬프트를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 프롬프트 설정을 확인합니다.

2.2 프롬프트 노드 설정

기본 설정:
-이름: 노드의 이름 (예: "시장 분석 프롬프트")
-설명: 노드의 설명 (선택사항)
-프롬프트: 등록된 프롬프트가 자동으로 로드됩니다

고급 설정:
-최대 토큰 수: 생성할 텍스트의 최대 길이
-온도: 생성 텍스트의 창의성 (0.0 ~ 1.0)
-입력 스키마: 프롬프트에 전달할 데이터 구조
-출력 스키마: 프롬프트가 생성할 데이터 구조

2.3 입력 데이터 매핑

이전 노드의 출력을 프롬프트 입력으로 매핑합니다:

```json
{
 "newsData": "$previousNodeId",
 "marketData": "$anotherNodeId"
}
```

💡 팁: `$노드ID` 형태로 이전 노드의 출력을 참조할 수 있습니다.

---

3. API 노드 (API Node)

용도: 외부 API를 호출하여 데이터를 가져옵니다.

3.1 API 노드 생성

1. 노드 팔레트에서 "API 호출" 카테고리를 찾습니다.
2. 등록된 API를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 API 설정을 확인합니다.

3.2 API 노드 설정

기본 설정:
-이름: 노드의 이름
-API 호출: 등록된 API가 자동으로 로드됩니다
-URL: API 엔드포인트 (등록된 API 사용 시 자동 설정)
-메서드: GET, POST, PUT, DELETE 등

요청 설정:
-헤더: API 요청 헤더 (JSON 형식)
-요청 본문: API 요청 데이터 (JSON 형식)
-파라미터 템플릿: 동적 파라미터 설정

응답 처리:
-응답 스키마: API 응답 데이터 구조
-전처리 프롬프트: API 호출 전 파라미터 생성
-후처리 프롬프트: API 응답 포맷팅

3.3 API 호출 예시

GET 요청:
```json
{
 "url": "https://api.example.com/data",
 "method": "GET",
 "headers": {
   "Authorization": "Bearer {token}"
 }
}
```

POST 요청:
```json
{
 "url": "https://api.example.com/analyze",
 "method": "POST",
 "headers": {
   "Content-Type": "application/json"
 },
 "body": {
   "text": "$previousNode.output"
 }
}
```

---

4. SQL 쿼리 노드 (SQL Query Node)

용도: 데이터베이스에서 SQL 쿼리를 실행하여 데이터를 조회합니다.

4.1 SQL 쿼리 노드 생성

1. 노드 팔레트에서 "SQL 쿼리" 카테고리를 찾습니다.
2. 등록된 SQL 쿼리를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 SQL 쿼리 설정을 확인합니다.

4.2 SQL 쿼리 노드 설정

기본 설정:
-이름: 노드의 이름
-SQL 쿼리: 등록된 SQL 쿼리가 자동으로 로드됩니다
-데이터소스: 쿼리를 실행할 데이터소스

파라미터 설정:
-파라미터: SQL 쿼리에 전달할 파라미터
-파라미터 매핑: 이전 노드의 출력을 파라미터로 매핑

4.3 SQL 쿼리 예시

기본 쿼리:
```sql
SELECT * FROM nh_ai.gold.macro_market_analysis
WHERE base_date >= current_date() - INTERVAL 7 DAYS
ORDER BY base_date DESC
LIMIT 100;
```

파라미터 사용:
```sql
SELECT * FROM nh_ai.gold.macro_market_analysis
WHERE base_date BETWEEN '{startDate}' AND '{endDate}'
ORDER BY base_date DESC
LIMIT {limit};
```

파라미터는 입력 매핑에서 설정합니다:
```json
{
 "startDate": "2025-01-01",
 "endDate": "2025-01-31",
 "limit": 100
}
```

---

5. 데이터소스 노드 (DataSource Node)

용도: 데이터소스에서 직접 데이터를 조회합니다.

5.1 데이터소스 노드 생성

1. 노드 팔레트에서 "데이터소스" 카테고리를 찾습니다.
2. 등록된 데이터소스를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 데이터소스 설정을 확인합니다.

5.2 데이터소스 노드 설정

기본 설정:
-이름: 노드의 이름
-데이터소스: 등록된 데이터소스 선택
-쿼리: SQL 쿼리 직접 입력
-출력 키: 결과 데이터의 키 이름

쿼리 작성 팁:
- SQL 쿼리를 직접 입력하거나
- 등록된 SQL 쿼리를 참조할 수 있습니다
- 스키마 브라우저에서 테이블 구조를 확인할 수 있습니다

---

6. Python 스크립트 노드 (Python Script Node)

용도: Python 코드를 실행하여 데이터를 처리합니다.

6.1 Python 스크립트 노드 생성

1. 노드 팔레트에서 "Python 스크립트" 카테고리를 찾습니다.
2. 등록된 Python 스크립트를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 Python 스크립트 설정을 확인합니다.

6.2 Python 스크립트 노드 설정

기본 설정:
-이름: 노드의 이름
-Python 스크립트: 등록된 스크립트가 자동으로 로드됩니다
-입력 형식: JSON, CSV, Text 등
-출력 형식: JSON, CSV, Text 등

실행 환경 설정:
-타임아웃: 스크립트 실행 최대 시간 (초)
-메모리 제한: 최대 메모리 사용량 (MB)
-CPU 제한: 최대 CPU 사용률 (%)

6.3 Python 스크립트 예시

입력 데이터 처리:
```python
import json

입력 데이터 받기
input_data = json.loads(input())

데이터 처리
result = {
   "processed": True,
   "data": input_data,
   "summary": f"Processed {len(input_data)} items"
}

출력
print(json.dumps(result))
```

주의사항:
- Python 스크립트는 안전한 샌드박스 환경에서 실행됩니다
- 파일 시스템 접근이 제한될 수 있습니다
- 네트워크 접근은 허용됩니다

---

7. RAG 노드 (RAG Node)

용도: RAG(Retrieval-Augmented Generation) 검색을 실행합니다.

7.1 RAG 노드 생성

1. 노드 팔레트에서 "RAG 검색" 카테고리를 찾습니다.
2. 등록된 RAG 스키마를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 RAG 설정을 확인합니다.

7.2 RAG 노드 설정

기본 설정:
-이름: 노드의 이름
-RAG 스키마: 등록된 RAG 스키마 선택
-검색 타입: Hybrid, Vector, Keyword
-Top K: 검색 결과 개수
-임계값: 검색 점수 임계값

검색 타입 설명:
-Hybrid: 벡터 검색 + 키워드 검색 (권장)
-Vector: 벡터 유사도 검색만
-Keyword: 키워드 검색만

7.3 RAG 검색 사용 예시

입력 데이터:
```json
{
 "query": "금리 인상이 주식 시장에 미치는 영향"
}
```

출력 데이터:
```json
{
 "query": "금리 인상이 주식 시장에 미치는 영향",
 "results": [
   {
     "id": "doc1",
     "content": "금리 인상은 주식 시장에 부정적 영향을...",
     "score": 0.95,
     "metadata": {
       "date": "2025-01-15",
       "category": "시장 분석"
     }
   }
 ],
 "totalCount": 10
}
```

---

8. 조건 노드 (Condition Node)

용도: 조건에 따라 다른 경로로 데이터를 전달합니다.

8.1 조건 노드 생성

1. 노드 팔레트에서 "조건 분기" 카테고리를 찾습니다.
2. 조건 노드를 더블클릭하거나 드래그하여 추가합니다.
3. 속성 패널에서 조건을 설정합니다.

8.2 조건 노드 설정

조건 표현식:
```javascript
// 예시 1: 숫자 비교
input.profit > 0

// 예시 2: 문자열 비교
input.status === "completed"

// 예시 3: 복합 조건
input.score > 80 && input.status === "active"
```

분기 설정:
-True 분기: 조건이 참일 때 연결할 노드
-False 분기: 조건이 거짓일 때 연결할 노드

---

9. 병합 노드 (Merge Node)

용도: 여러 노드의 출력을 하나로 병합합니다.

9.1 병합 노드 생성

1. 노드 팔레트에서 "데이터 병합" 카테고리를 찾습니다.
2. 병합 노드를 더블클릭하거나 드래그하여 추가합니다.
3. 여러 노드의 출력을 병합 노드로 연결합니다.

9.2 병합 노드 설정

병합 방식:
-자동 병합: 노드 ID를 키로 사용하여 자동 병합
-수동 병합: 출력 매핑에서 직접 설정

출력 형식:
```json
{
 "node1": { /* node1의 출력 */ },
 "node2": { /* node2의 출력 */ },
 "node3": { /* node3의 출력 */ }
}
```

---

10. 종료 노드 (End Node)

용도: 워크플로우의 종료점을 나타냅니다.

설정:
- 이름: 워크플로우 종료점
- 설명: (선택사항)

사용 예시:
- 워크플로우의 마지막 노드로 사용
- 최종 결과를 저장하거나 전송할 때 사용

---

노드 관리 기능

노드 선택 및 편집

노드 선택

1. 캔버스에서 노드를 클릭합니다.
2. 속성 패널이 자동으로 열립니다.
3. 노드의 설정을 확인하고 수정할 수 있습니다.

노드 편집

1. 노드를 선택합니다.
2. 속성 패널에서 설정을 수정합니다.
3. "적용" 버튼을 클릭하여 저장합니다.

노드 삭제

1. 노드를 선택합니다.
2. 키보드의 Delete 키를 누르거나
3. 노드의 삭제 버튼을 클릭합니다.
4. 확인 대화상자에서 "삭제"를 클릭합니다.

💡 주의: 노드를 삭제하면 연결된 모든 연결선도 함께 삭제됩니다.

노드 이동 및 크기 조정

노드 이동

1. 노드를 드래그하여 원하는 위치로 이동합니다.
2. 노드 위치는 자동으로 저장됩니다.

노드 정렬

1. 여러 노드를 선택합니다 (Ctrl/Cmd + 클릭).
2. 우클릭 메뉴에서 "정렬" 옵션을 선택합니다.

노드 복사 및 붙여넣기

1. 노드를 선택합니다.
2. Ctrl/Cmd + C로 복사합니다.
3. Ctrl/Cmd + V로 붙여넣기합니다.
4. 새 노드가 생성되며 ID는 자동으로 변경됩니다.

---

데이터 조회 기능

노드 데이터 조회

워크플로우 편집기에서 각 노드의 중간 결과를 실시간으로 확인할 수 있습니다.

데이터 조회 방법

1. 노드를 선택합니다.
2. 속성 패널 하단의 "노드 데이터 조회" 버튼을 클릭합니다.
3. 데이터 조회 다이얼로그가 열립니다.

자동 의존성 처리

데이터 조회 시 이전 노드가 실행되지 않았으면 자동으로 실행됩니다:

1. "노드 데이터 조회" 버튼을 클릭합니다.
2. 시스템이 이전 노드의 실행 상태를 확인합니다.
3. 실행되지 않은 이전 노드가 있으면 자동으로 실행합니다.
4. 진행 상태가 표시됩니다:
  - "이전 노드 실행 중: node1..."
  - "현재 노드 실행 중: node2..."
  - "데이터 조회 중..."

데이터 조회 결과 확인

데이터 조회 다이얼로그에서 다음을 확인할 수 있습니다:

-테이블 형식 데이터: SQL 쿼리 결과 등
-JSON 형식 데이터: API 응답, 프롬프트 출력 등
-실행 시간: 노드 실행에 걸린 시간
-행 수: 조회된 데이터의 개수

노드 타입별 데이터 조회

SQL 쿼리 노드:
- SQL 쿼리를 실행하여 결과를 조회합니다.
- Databricks에서 직접 데이터를 가져옵니다.

프롬프트 노드:
- 프롬프트 실행 결과를 조회합니다.
- 생성된 텍스트를 확인할 수 있습니다.

API 노드:
- API 호출 결과를 조회합니다.
- 응답 데이터를 확인할 수 있습니다.

기타 노드:
- 노드 실행 결과를 조회합니다.
- 출력 데이터를 확인할 수 있습니다.

---

워크플로우 구성 요소 관리

워크플로우에서 사용하는 프롬프트, API, Python 스크립트, SQL 쿼리 등을 관리할 수 있습니다.

프롬프트 관리

프롬프트 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "프롬프트 관리" 메뉴를 클릭합니다.

새 프롬프트 생성

1. "새 프롬프트" 버튼을 클릭합니다.
2. 프롬프트 정보를 입력합니다:
  - 이름: 프롬프트 이름
  - 설명: 프롬프트 설명
  - 카테고리: 프롬프트 카테고리
  - 시스템 프롬프트: AI에게 역할을 부여하는 프롬프트
  - 사용자 프롬프트 템플릿: 실제 작업을 수행하는 프롬프트
3. "저장" 버튼을 클릭합니다.

프롬프트 편집

1. 프롬프트 목록에서 편집할 프롬프트를 찾습니다.
2. "편집" 버튼을 클릭합니다.
3. 프롬프트 정보를 수정합니다.
4. "저장" 버튼을 클릭합니다.

프롬프트 삭제

1. 프롬프트 목록에서 삭제할 프롬프트를 찾습니다.
2. "삭제" 버튼을 클릭합니다.
3. 확인 대화상자에서 "삭제"를 클릭합니다.

💡 주의: 워크플로우에서 사용 중인 프롬프트는 삭제할 수 없습니다.

프롬프트 사용 팁

템플릿 변수 사용:
```
다음 뉴스 데이터를 분석해주세요:
{newsData}

시장 상황:
{marketData}
```

템플릿 변수는 워크플로우에서 입력 매핑으로 설정할 수 있습니다.

프롬프트 작성 가이드:
1. 명확한 지시사항: AI가 수행할 작업을 명확히 설명합니다.
2. 구조화된 출력: JSON 형식 등 구조화된 출력을 요청합니다.
3. 예시 제공: 원하는 출력 형식의 예시를 포함합니다.
4. 제약 조건: 출력 길이, 형식 등의 제약 조건을 명시합니다.

예시 프롬프트:
```
당신은 금융 시장 분석 전문가입니다. 
다음 뉴스 데이터를 분석하여 시장에 영향을 미치는 주요 이벤트를 추출해주세요.

입력 데이터
{newsData}

출력 형식
{
 "events": [
   {
     "title": "이벤트 제목",
     "impact": "높음/중간/낮음",
     "relatedStocks": ["종목1", "종목2"],
     "summary": "이벤트 요약"
   }
 ],
 "summary": "전체 요약"
}

분석 기준
- 시장에 직접적인 영향을 미치는 이벤트만 추출
- 영향도는 객관적인 기준으로 평가
- 관련 종목은 최대 5개까지
```

---

API 관리

API 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "API 호출 관리" 메뉴를 클릭합니다.

새 API 생성

1. "새 API" 버튼을 클릭합니다.
2. API 정보를 입력합니다:
  - 이름: API 이름
  - 설명: API 설명
  - 카테고리: API 카테고리
  - URL: API 엔드포인트
  - 메서드: GET, POST, PUT, DELETE 등
  - 헤더: 요청 헤더 (JSON 형식)
  - 요청 스키마: 요청 데이터 구조
  - 응답 스키마: 응답 데이터 구조
3. "저장" 버튼을 클릭합니다.

API 테스트

1. API 목록에서 테스트할 API를 찾습니다.
2. "테스트" 버튼을 클릭합니다.
3. 테스트 데이터를 입력합니다.
4. "실행" 버튼을 클릭합니다.
5. 응답 결과를 확인합니다.

API 인증 설정

Bearer Token:
```json
{
 "Authorization": "Bearer {token}"
}
```

API Key:
```json
{
 "X-API-Key": "{apiKey}"
}
```

---

Python 스크립트 관리

Python 스크립트 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "Python 스크립트 관리" 메뉴를 클릭합니다.

새 Python 스크립트 생성

1. "새 Python 스크립트" 버튼을 클릭합니다.
2. 스크립트 정보를 입력합니다:
  - 이름: 스크립트 이름
  - 설명: 스크립트 설명
  - Python 스크립트: Python 코드
  - 필요 패키지: 필요한 Python 패키지 목록
3. "저장" 버튼을 클릭합니다.

Python 스크립트 테스트

1. 스크립트 목록에서 테스트할 스크립트를 찾습니다.
2. "테스트" 버튼을 클릭합니다.
3. 테스트 입력 데이터를 입력합니다.
4. "실행" 버튼을 클릭합니다.
5. 실행 결과를 확인합니다.

Python 스크립트 작성 팁

입력 데이터 처리:
```python
import json
import sys

표준 입력에서 데이터 읽기
input_data = json.loads(sys.stdin.read())

데이터 처리
result = process_data(input_data)

표준 출력으로 결과 출력
print(json.dumps(result))
```

에러 처리:
```python
try:
   result = process_data(input_data)
except Exception as e:
   error_result = {
       "error": str(e),
       "success": False
   }
   print(json.dumps(error_result))
   sys.exit(1)
```

---

SQL 쿼리 관리

SQL 쿼리 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "데이터소스 관리" 메뉴를 클릭합니다.
3. "SQL 쿼리" 탭을 클릭합니다.

새 SQL 쿼리 생성

1. "새 SQL 쿼리" 버튼을 클릭합니다.
2. 쿼리 정보를 입력합니다:
  - 이름: 쿼리 이름
  - 설명: 쿼리 설명
  - 데이터소스: 쿼리를 실행할 데이터소스
  - SQL 쿼리: SQL 쿼리 코드
  - 파라미터: 쿼리 파라미터 목록
3. "저장" 버튼을 클릭합니다.

SQL 쿼리 테스트

1. 쿼리 목록에서 테스트할 쿼리를 찾습니다.
2. "테스트" 버튼을 클릭합니다.
3. 파라미터 값을 입력합니다 (있는 경우).
4. "실행" 버튼을 클릭합니다.
5. 실행 결과를 확인합니다.

SQL 쿼리 작성 팁

파라미터 사용:
```sql
SELECT * FROM nh_ai.gold.macro_market_analysis
WHERE base_date BETWEEN '{startDate}' AND '{endDate}'
ORDER BY base_date DESC
LIMIT {limit};
```

스키마 조회:
- 데이터소스 관리 페이지에서 "스키마 조회" 버튼을 클릭합니다.
- 테이블과 컬럼 정보를 확인할 수 있습니다.
- 테이블/컬럼을 클릭하면 SQL 쿼리에 자동으로 추가됩니다.

---

데이터소스 관리

데이터소스 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "데이터소스 관리" 메뉴를 클릭합니다.

새 데이터소스 생성

1. "새 데이터소스" 버튼을 클릭합니다.
2. 데이터소스 정보를 입력합니다:
  - 이름: 데이터소스 이름
  - 타입: Databricks, PostgreSQL, CosmosDB 등
  - 연결 정보: 호스트, 포트, 데이터베이스 등
  - 인증 정보: 사용자명, 비밀번호 등
3. "저장" 버튼을 클릭합니다.

데이터소스 테스트

1. 데이터소스 목록에서 테스트할 데이터소스를 찾습니다.
2. "연결 테스트" 버튼을 클릭합니다.
3. 연결 상태를 확인합니다.

스키마 조회

1. 데이터소스 목록에서 스키마를 조회할 데이터소스를 찾습니다.
2. "스키마 조회" 버튼을 클릭합니다.
3. 스키마 다이얼로그에서 다음을 확인할 수 있습니다:
  - 카탈로그/데이터베이스: 데이터베이스 목록
  - 스키마: 스키마 목록
  - 테이블: 테이블 목록
  - 컬럼: 컬럼 목록 및 타입

💡 팁: SQL 쿼리 입력 시 스키마 조회 다이얼로그를 열어두면 테이블/컬럼을 클릭하여 쿼리에 자동으로 추가할 수 있습니다.

---

워크플로우 실행 및 테스트

워크플로우 실행

전체 워크플로우 실행

1. 상단의 "실행" 버튼(재생 아이콘)을 클릭합니다.
2. 워크플로우가 순차적으로 실행됩니다.
3. 각 노드의 실행 상태가 표시됩니다.

단일 노드 실행 (시뮬레이션 모드)

1. 상단의 "시뮬레이션 모드" 토글을 켭니다.
2. 실행할 노드를 선택합니다.
3. 노드의 "실행" 버튼을 클릭합니다.
4. 노드 실행 결과를 확인합니다.

워크플로우 테스트

테스트 실행

1. 상단의 "테스트 실행" 버튼(플라스크 아이콘)을 클릭합니다.
2. 테스트 입력 데이터를 입력합니다 (선택사항).
3. "실행" 버튼을 클릭합니다.
4. 테스트 결과를 확인합니다.

실행 결과 확인

1. 테스트 실행 후 "결과 보기" 버튼을 클릭합니다.
2. 각 노드의 실행 결과를 확인할 수 있습니다:
  - 입력 데이터: 노드에 전달된 입력
  - 출력 데이터: 노드가 생성한 출력
  - 실행 시간: 노드 실행에 걸린 시간
  - 에러 메시지: 실행 실패 시 에러 정보

---

고급 기능

워크플로우 템플릿

템플릿 사용하기

1. 상단의 "템플릿" 버튼을 클릭합니다.
2. 템플릿 목록에서 원하는 템플릿을 선택합니다.
3. "이 템플릿 사용" 버튼을 클릭합니다.
4. 템플릿이 캔버스에 로드됩니다.

제공되는 템플릿

-일일 시장 분석 리포트: 매일 시장 데이터를 수집하고 분석
-테마별 뉴스 수집 및 요약: 특정 테마의 뉴스를 수집하고 요약
-실시간 가격 모니터링 및 알림: 주요 종목의 가격을 모니터링하고 알림
-RAG 기반 투자 인사이트: RAG 엔진을 활용한 맞춤형 인사이트
-AI 시황 생성: 뉴스 데이터를 수집하고 AI로 분석하여 시황 생성

워크플로우 폴더 관리

폴더 생성

1. 워크플로우 목록에서 "폴더 생성" 버튼을 클릭합니다.
2. 폴더 이름을 입력합니다.
3. "생성" 버튼을 클릭합니다.

워크플로우를 폴더로 이동

1. 워크플로우를 선택합니다.
2. 상단의 "폴더" 드롭다운을 클릭합니다.
3. 원하는 폴더를 선택합니다.

노드 생성 (캔버스에서)

오른쪽 클릭으로 노드 생성

1. 캔버스의 빈 공간에서 오른쪽 클릭합니다.
2. "노드 생성" 메뉴를 선택합니다.
3. 노드 팔레트가 표시됩니다.
4. 원하는 노드를 선택합니다.
5. 노드가 클릭한 위치에 생성됩니다.

---

데이터 생성 결과 조회

데이터생성 확인 페이지

워크플로우 실행 결과를 Databricks에서 조회할 수 있는 전용 페이지가 제공됩니다.

AI 시황 결과 데이터 쿼리

1. 왼쪽 사이드바에서 "데이터 관리" 카테고리를 클릭합니다.
2. "AI 시황 결과 데이터 쿼리" 메뉴를 클릭합니다.

기능:
- 쿼리 템플릿 선택 (기본 조회, 날짜 범위, 키워드 검색 등)
- 조회 조건 설정 (날짜 범위, 키워드, 제한 등)
- SQL 쿼리 편집 및 실행
- 결과 테이블 표시
- CSV 내보내기

사용 방법:
1. 쿼리 템플릿을 선택합니다.
2. 조회 조건을 설정합니다 (날짜 범위, 키워드 등).
3. "조건 적용" 버튼을 클릭하여 쿼리를 생성합니다.
4. "실행" 버튼을 클릭하여 쿼리를 실행합니다.
5. 결과를 확인하고 필요시 "CSV 내보내기"를 클릭합니다.

잔고 분석 결과 데이터 쿼리

1. 왼쪽 사이드바에서 "데이터 관리" 카테고리를 클릭합니다.
2. "잔고 분석 결과 데이터 쿼리" 메뉴를 클릭합니다.

기능:
- 쿼리 템플릿 선택 (기본 조회, 사용자별, 수익률 필터 등)
- 조회 조건 설정 (사용자 ID, 날짜 범위, 수익률, 리스크 점수 등)
- SQL 쿼리 편집 및 실행
- 결과 테이블 표시
- CSV 내보내기

사용 방법:
1. 쿼리 템플릿을 선택합니다.
2. 조회 조건을 설정합니다 (사용자 ID, 수익률 범위 등).
3. "조건 적용" 버튼을 클릭하여 쿼리를 생성합니다.
4. "실행" 버튼을 클릭하여 쿼리를 실행합니다.
5. 결과를 확인하고 필요시 "CSV 내보내기"를 클릭합니다.

---

문제 해결

일반적인 문제

노드가 실행되지 않습니다

원인:
- 이전 노드가 실행되지 않았을 수 있습니다.
- 노드 설정이 올바르지 않을 수 있습니다.

해결 방법:
1. 이전 노드를 먼저 실행합니다.
2. 노드 설정을 확인합니다.
3. 에러 메시지를 확인합니다.

데이터가 전달되지 않습니다

원인:
- 노드 간 연결이 올바르지 않을 수 있습니다.
- 입력 매핑이 올바르지 않을 수 있습니다.

해결 방법:
1. 노드 간 연결선을 확인합니다.
2. 입력 매핑을 확인합니다.
3. 이전 노드의 출력을 확인합니다.

SQL 쿼리가 실행되지 않습니다

원인:
- SQL 구문 오류가 있을 수 있습니다.
- 데이터소스 연결이 끊어졌을 수 있습니다.

해결 방법:
1. SQL 구문을 확인합니다.
2. 데이터소스 연결을 확인합니다.
3. 스키마 브라우저에서 테이블 구조를 확인합니다.

프롬프트가 실행되지 않습니다

원인:
- 프롬프트가 등록되지 않았을 수 있습니다.
- 입력 데이터가 올바르지 않을 수 있습니다.

해결 방법:
1. 프롬프트 관리에서 프롬프트를 확인합니다.
2. 입력 매핑을 확인합니다.
3. 프롬프트 템플릿 변수를 확인합니다.

에러 메시지 해석

"노드를 찾을 수 없습니다"

의미: 워크플로우 정의에 노드가 없습니다.

해결 방법: 노드를 다시 추가하거나 워크플로우를 다시 로드합니다.

"이전 노드가 실행되지 않았습니다"

의미: 현재 노드를 실행하기 위해 필요한 이전 노드가 실행되지 않았습니다.

해결 방법: 이전 노드를 먼저 실행하거나 전체 워크플로우를 실행합니다.

"SQL 쿼리 실행 실패"

의미: SQL 쿼리에 오류가 있거나 데이터소스 연결에 문제가 있습니다.

해결 방법: 
1. SQL 구문을 확인합니다.
2. 데이터소스 연결을 확인합니다.
3. 쿼리 로그를 확인합니다.

---

RAG 및 벡터 임베딩

RAG 시스템 개요

RAG(Retrieval-Augmented Generation)는 데이터를 벡터로 변환하여 AI 검색에 활용하는 시스템입니다. Databricks의 데이터를 벡터 임베딩하여 AI Search에서 검색할 수 있도록 합니다.

RAG 관리 페이지 접근

1. 왼쪽 사이드바에서 "AI 시스템 관리" 카테고리를 클릭합니다.
2. "RAG 관리" 메뉴를 클릭합니다.

RAG 스키마 생성

1. 새 RAG 스키마 생성

1. "새 RAG 스키마" 버튼을 클릭합니다.
2. 스키마 정보를 입력합니다:
  - 이름: RAG 스키마 이름
  - 설명: 스키마 설명
  - Databricks 카탈로그: 데이터가 있는 카탈로그
  - Databricks 스키마: 데이터가 있는 스키마
  - Databricks 테이블: 벡터 임베딩할 테이블
  - Databricks 쿼리: (선택사항) 커스텀 쿼리
  - 임베딩 모델: 사용할 임베딩 모델 (기본: text-embedding-3-large)
  - 임베딩 필드: 임베딩할 컬럼명
  - AI Search 인덱스 이름: AI Search 인덱스 이름
3. "저장" 버튼을 클릭합니다.

2. 임베딩 작업 실행

1. RAG 스키마 목록에서 작업할 스키마를 찾습니다.
2. "임베딩 작업 실행" 버튼을 클릭합니다.
3. 작업 타입을 선택합니다:
  - 전체 임베딩: 모든 데이터를 처음부터 임베딩
  - 증분 임베딩: 새로 추가된 데이터만 임베딩
  - 수동 임베딩: 특정 조건의 데이터만 임베딩
4. "실행" 버튼을 클릭합니다.
5. 작업 진행 상황을 확인합니다.

3. 임베딩 상태 확인

RAG 관리 페이지에서 다음을 확인할 수 있습니다:

-최신 데이터 임베딩 시간: 마지막으로 임베딩된 시간
-임베딩된 레코드 수: 현재 임베딩된 데이터 개수
-전체 레코드 수: 소스 테이블의 전체 데이터 개수
-진행률: 임베딩 진행률 (%)

4. RAG 검색 사용

워크플로우에서 RAG 노드를 사용하여 벡터 검색을 수행할 수 있습니다:

1. 워크플로우 편집기에서 RAG 노드를 추가합니다.
2. RAG 스키마를 선택합니다.
3. 검색 타입을 선택합니다 (Hybrid, Vector, Keyword).
4. 검색 쿼리를 입력합니다.
5. 워크플로우를 실행하면 RAG 검색이 수행됩니다.

---

부록

키보드 단축키

-Ctrl/Cmd + S: 워크플로우 저장
-Ctrl/Cmd + Z: 실행 취소
-Ctrl/Cmd + Y: 다시 실행
-Delete: 선택한 노드 삭제
-Ctrl/Cmd + C: 노드 복사
-Ctrl/Cmd + V: 노드 붙여넣기

워크플로우 모범 사례

1. 명확한 노드 이름 사용: 각 노드에 의미 있는 이름을 부여합니다.
2. 설명 추가: 복잡한 노드는 설명을 추가합니다.
3. 에러 처리: 중요한 노드는 에러 처리를 추가합니다.
4. 테스트: 워크플로우를 실행하기 전에 테스트합니다.
5. 버전 관리: 중요한 워크플로우는 정기적으로 저장합니다.

추가 리소스

-워크플로우 모니터: 워크플로우 실행 상태를 모니터링합니다.
-스케줄러: 워크플로우를 자동으로 실행합니다.
-로그 뷰어: 워크플로우 실행 로그를 확인합니다.

---

자주 묻는 질문 (FAQ)

Q1: 워크플로우를 실행했는데 노드가 실행되지 않습니다.

A: 이전 노드가 실행되지 않았을 수 있습니다. 노드 데이터 조회 기능을 사용하면 이전 노드가 자동으로 실행됩니다.

Q2: SQL 쿼리를 작성할 때 테이블 구조를 확인하고 싶습니다.

A: 데이터소스 관리 페이지에서 "스키마 조회" 버튼을 클릭하면 테이블과 컬럼 정보를 확인할 수 있습니다. 테이블/컬럼을 클릭하면 SQL 쿼리에 자동으로 추가됩니다.

Q3: 워크플로우 실행 결과를 어디서 확인할 수 있나요?

A: 
1. 워크플로우 편집기: 각 노드의 데이터 조회 기능 사용
2. 데이터생성 확인 페이지: AI 시황 결과 데이터 쿼리, 잔고 분석 결과 데이터 쿼리
3. 워크플로우 모니터: 전체 워크플로우 실행 상태 확인

Q4: RAG 검색을 사용하려면 어떻게 해야 하나요?

A: 
1. RAG 관리 페이지에서 RAG 스키마를 생성합니다.
2. Databricks 테이블을 지정하고 임베딩 작업을 실행합니다.
3. 워크플로우에서 RAG 노드를 추가하고 스키마를 선택합니다.

Q5: 프롬프트 템플릿 변수는 어떻게 사용하나요?

A: 프롬프트 템플릿에서 `{변수명}` 형태로 변수를 사용하고, 워크플로우 노드의 입력 매핑에서 값을 설정합니다.

---

문서 버전: 1.0  
최종 업데이트: 2025-11-28  
작성자: AI Assistant

💡 도움이 필요하신가요?  
문의사항이 있으시면 개발팀에 연락해주세요.

